# Backend Express Node.js

## Jalankan

1. Copy `.env.example` ke `.env`.
2. Install dependency:
   - `npm install`
3. Run dev mode:
   - `npm run dev`

## Endpoint awal (Express)

- `GET /health`
- `GET /api`
